# Reference list of required secrets (documentation only).
# On Cloudflare: local dev -> `.dev.vars`; production -> `wrangler secret put NAME`.
# No Claude API - this app is pure event-mapping.
TXLINE_API_KEY=your_txline_key
